#!/usr/bin/env bash

set -euo pipefail

source "$(dirname "$0")/../load-host-env.sh"

if [[ -t 0 ]]; then
  exec ssh -t "lima-${LIMA_INSTANCE}" bash -li
else
  exec ssh "lima-${LIMA_INSTANCE}" bash -l
fi
